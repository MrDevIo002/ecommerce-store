import Carousel from "@/ui/Carousel";
import Container from "../Container";

const Banner = () => {
   return (
      <Container>
         <Carousel />
      </Container>
   );
};

export default Banner;
